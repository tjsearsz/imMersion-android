mutation addCompany($input: CreateCompanyInput!) {
    createCompany(createCompanyInput: $input){
        name
        description
        _id
        companySector
    }
}

query getCompanies {
    userCompanies{
        description,
        name,
        _id
        companySector
    }
}